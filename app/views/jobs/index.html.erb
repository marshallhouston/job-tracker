<table>
  <tr>
    <th>City</th>
    <th>Job</th>
  </tr>

  <% @jobs.each do |job| %>
  <tr>
    <td><%= job.city %></td>
    <td><%= link_to job.title, job_path(job) %></td>
  </tr>
  <% end %>
</table>
